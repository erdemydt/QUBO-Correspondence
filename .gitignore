# Globbed so scratch trees (build-debug/, build2/) are covered too.
build*/
out/

# CMake artifacts
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json
_deps/
Testing/

# Mesh datasets: large (~225 MB total) and external inputs, not source. A new
# dataset needs a line here and a row in kDatasetDirs in src/dataset.cpp.
scape-bim/
tosca-offs/

# Editor / OS
.vscode/
.idea/
.cache/
*.swp
.DS_Store
